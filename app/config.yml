development:
    host: localhost
    root: root
    password: password
    suffix: dev
production:
    host: production.test.host.name
    root: root
    password: password
production_test:
    host: production.test.host.name
    root: root
    password: password
    suffix: test